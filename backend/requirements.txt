fastapi==0.116.1
uvicorn[standard]==0.35.0
google-genai>=1.0.0,<3.0.0
httpx>=0.27,<1.0
ai-edge-litert==2.2.0
numpy>=2.0,<3.0
pillow>=11.0,<13.0
qrcode>=8.0,<9.0
# Speaker identification (ECAPA-TDNN via speechbrain).  torch is CPU-only
# on idc01 (no GPU); the default Linux wheel works but is ~2.5 GB.  For a
# smaller install use: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1,<3.0
speechbrain>=1.0,<2.0
